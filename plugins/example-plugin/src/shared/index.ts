// Plugin route definitions
// Export $route definitions here to share between client and server
//
// Example:
// export const getProducts = $route({
//   path: () => '/products',
//   body: z.never(),
//   query: z.object({ limit: z.number().optional() }),
//   data: z.array(productSchema),
//   errorCodes: [],
// });
